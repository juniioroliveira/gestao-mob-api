{
  "name": "gestao-mob-backend",
  "version": "1.0.0",
  "description": "Servidor Backend para o app Gestão Mob",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "git add -A && git diff --cached --quiet || (git commit -m \"chore: auto-commit changes\" && git push origin main)"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.7.5",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
